# AGENTS.md

このリポジトリで Codex が作業するときの基本ルールです。より深い階層に別の `AGENTS.md` がある場合は、その内容もあわせて守ってください。

## コミュニケーション
- 返答・進捗報告は原則として日本語で行う。
- 作業前に必要な範囲で既存ファイルを確認し、既存の設計・命名・文体に合わせる。
- 判断に迷う点があっても、影響が小さく戻しやすい場合は合理的に進め、あとで変更内容を説明する。
- 仕様・デザイン・公開範囲・削除など、戻しにくい判断が必要な場合はユーザーに確認する。

## 許可なく実行してよいこと
- ファイルやディレクトリの一覧取得、検索、内容確認。
- `git status`、`git diff`、`git log` などの読み取り専用 Git コマンド。
- 既存コードの小さな修正、ドキュメント更新、型・lint エラー修正。
- 既存の `package.json` にある `lint`、`build`、`test` 相当の検証コマンドの実行。
- ローカル開発サーバーの起動。ただし長時間動かす場合やポート変更が必要な場合は状況を共有する。
- 作業用の一時ファイル作成。ただし不要になったものは片付ける。

## ユーザー確認が必要なこと
- 新しい依存パッケージの追加・更新・削除。
- ネットワークアクセスが必要な操作。
- `git commit`、`git push`、ブランチ作成・切り替え・マージ・リベースなど履歴や作業ブランチに影響する操作。
- ファイルやディレクトリの削除、大量移動、大規模な自動整形。
- `.env`、秘密鍵、API キー、認証情報、デプロイ設定に関わる変更。
- 本番公開、デプロイ、外部サービス設定の変更。
- 画像・動画など重いアセットの圧縮、生成、置換。

## 禁止・注意事項
- ユーザーの未コミット変更を勝手に戻さない。
- `git reset --hard`、`git checkout -- <path>`、強制 push などの破壊的操作は、明示的な依頼がない限り行わない。
- 秘密情報をログ・ドキュメント・コミット対象に含めない。
- 文字化けを見つけた場合は、文字コードを確認してから編集する。

## このプロジェクト固有のメモ
- メインの Next.js アプリは `hasuda-portfolio/` 配下にある。
- `hasuda-portfolio/AGENTS.md` の Next.js ルールを守り、コード変更前に必要に応じて `node_modules/next/dist/docs/` を確認する。
- `docs/` 配下は作業計画やメモを置く場所として扱う。
